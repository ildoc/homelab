apiVersion: gateway.networking.k8s.io/v1alpha2
kind: TLSRoute
metadata:
  name: argocd-route
  namespace: argocd
spec:
  parentRefs:
    - name: cilium-gateway
      kind: Gateway
      namespace: kube-system
  hostnames:
    - "argocd.local.ildoc.it"
  rules:
    - backendRefs:
        - name: argocd-server
          port: 443
