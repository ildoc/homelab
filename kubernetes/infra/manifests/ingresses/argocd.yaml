# ---
# apiVersion: gateway.networking.k8s.io/v1
# kind: HTTPRoute
# metadata:
#   name: argocd-server
#   namespace: argocd
#   annotations:
#     # Cilium usa questa annotation per backend HTTPS
#     io.cilium.service/lb-l7-backend-protocol: "https"
# spec:
#   parentRefs:
#     - name: cilium-gateway
#       namespace: kube-system
#       sectionName: https
#   hostnames:
#     - "argocd.local.ildoc.it"
#   rules:
#     - matches:
#         - path:
#             type: PathPrefix
#             value: /
#       backendRefs:
#         - name: argocd-server
#           port: 443
