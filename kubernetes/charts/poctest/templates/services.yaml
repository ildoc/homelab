---
apiVersion: v1
kind: Service
metadata:
  name: pocmanager
  namespace: apps
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
  selector:
    app: pocmanager
---
apiVersion: v1
kind: Service
metadata:
  name: pocsender
  namespace: apps
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
  selector:
    app: pocsender
