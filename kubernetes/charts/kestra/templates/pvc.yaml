---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: kestra-storage
  namespace: apps
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: nfs-csi
  resources:
    requests:
      storage: 20Gi
