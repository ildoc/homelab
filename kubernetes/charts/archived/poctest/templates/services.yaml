---
apiVersion: v1
kind: Service
metadata:
  name: pocmanager
  namespace: apps
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
  selector:
    app: pocmanager
---
apiVersion: v1
kind: Service
metadata:
  name: pocsender
  namespace: apps
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
  selector:
    app: pocsender
