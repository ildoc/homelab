---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: matrix-synapse
  namespace: apps
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 50Gi
  storageClassName: nfs-csi
