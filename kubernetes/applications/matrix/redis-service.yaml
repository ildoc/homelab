---
apiVersion: v1
kind: Service
metadata:
  name: matrix-redis
  namespace: apps
  annotations:
    argocd.argoproj.io/sync-wave: "2"
spec:
  type: ClusterIP
  ports:
    - port: 6379
      targetPort: 6379
      protocol: TCP
      name: redis
  selector:
    app: matrix-redis
