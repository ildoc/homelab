---
apiVersion: v1
kind: Service
metadata:
  name: matrix-synapse
  namespace: apps
  labels:
    app: matrix-synapse
  annotations:
    argocd.argoproj.io/sync-wave: "4"
spec:
  type: ClusterIP
  ports:
    - port: 8008
      targetPort: 8008
      protocol: TCP
      name: http
    - port: 8448
      targetPort: 8448
      protocol: TCP
      name: federation
    - port: 9000
      targetPort: 9000
      protocol: TCP
      name: metrics
  selector:
    app: matrix-synapse
