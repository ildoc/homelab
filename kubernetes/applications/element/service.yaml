---
apiVersion: v1
kind: Service
metadata:
  name: element-web
  namespace: apps
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    app: element-web
